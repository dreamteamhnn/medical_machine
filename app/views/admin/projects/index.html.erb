<div id="page-wrapper">
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">Dự án tiêu biểu</h1>
    </div>
  </div>
  <div class="row" id="project-table">
    <div class="col-lg-12">
      <div class="create-div">
        <%= link_to "Tạo mới", new_admin_project_path(), class: "btn btn-success" %>
      </div>
      <%= render "shared/flash" %>
      <div class="panel-body table-responsive">
        <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-project">
          <thead>
            <tr>
              <th>STT</th>
              <th>Ảnh đại diện</th>
              <th>Tiêu đề</th>
              <th>Thứ tự trang chủ</th>
              <th class="center"></th>
            </tr>
          </thead>
          <tbody>
            <% @projects.each do |project| %>
              <tr class="odd gradeX" id="project<%= project.id %>">
                <td style="width: 30px !important">
                  <p><%= project.order %></p>
                </td>
                <td style="width: 100px !important">
                  <%= image_tag project.img.url, height: 100 %>
                </td>
                <td>
                  <p><%= project.title %></p>
                </td>
                <td style="width: 150px !important">
                  <p><%= project.order %></p>
                </td>
                <td class="center" style="width: 150px !important">
                  <%= link_to "Sửa", edit_admin_project_path(project), class: "btn btn-primary" %>
                  <button class="btn btn-danger button-delete-project" data-title="Xóa" data-toggle="modal" data-target="#delete-project" id="delete-project<%= project.id %>" >
                    Xóa
                  </button>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="modal fade" id="delete-project" tabindex="-1" role="dialog" aria-labelledby="delete" aria-hidden="true">
    <input type="hidden" class="delete-project-id" value="Norway">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <a href="#" class="close" data-dismiss="modal" aria-label="close">&times;</a>
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
          <h4 class="modal-title custom_align" id="Heading">Xóa dự án</h4>
        </div>
        <div class="modal-body">
          <div class="alert alert-danger">Bạn có chắc chắn xóa dự án <strong class="delete-project-name"></strong> không?</div>
        </div>
        <div class="modal-footer ">
          <button type="button" class="btn btn-success" id="button-delete-project-confirm" >Có</button>
          <button type="button" class="btn btn-default" data-dismiss="modal">Không</button>
        </div>
      </div>
    </div>
  </div>
</div>
